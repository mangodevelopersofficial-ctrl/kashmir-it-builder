name: Mango Auto Builder
on:
  workflow_dispatch:
    inputs:
      app_name:
        description: 'App Name'
        required: true
      package_name:
        description: 'Package ID'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Create Structure & Inject Assets
        run: |
          # Create Android folders automatically
          mkdir -p app/src/main/res/mipmap-xxxhdpi
          mkdir -p app/src/main/res/drawable
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/assets

          # Move files from root to Android structure
          [ -f "icon.png" ] && cp icon.png app/src/main/res/mipmap-xxxhdpi/ic_launcher.png || echo "Default icon"
          [ -f "index.html" ] && cp index.html app/src/main/assets/index.html || echo "Default HTML"
          
          # Create strings.xml for App Name
          echo '<?xml version="1.0" encoding="utf-8"?><resources><string name="app_name">${{ github.event.inputs.app_name }}</string></resources>' > app/src/main/res/values/strings.xml

      - name: Final Verification
        run: |
          echo "Building App: ${{ github.event.inputs.app_name }}"
          echo "Package ID: ${{ github.event.inputs.package_name }}"
          ls -R app/src/main/
